export enum LightColor {
  Green = 0,
  Yellow = 1,
  Red = 2,
}

export type Layout = "horizontal" | "vertical";

export const defaultLightInterval = [3000, 1500, 3000];
export const defaultBlinkInterval = 1000;

export const glowIntensity = 5;
